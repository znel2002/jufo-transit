# Logger only -- installed on every 15-minute CI poll, so keep it minimal.
# Analysis dependencies live in requirements-analysis.txt.
httpx>=0.27

# VBB GTFS-Realtime fallback source (see transit_logger/gtfsrt.py)
gtfs-realtime-bindings>=1.0
